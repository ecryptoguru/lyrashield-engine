// License signature verify submodules — placeholders that re-export the
// shared logic. The actual tests live in lib.rs.

#[cfg(test)]
mod tests {
    // All license tests are in lib.rs to share helpers.
}
